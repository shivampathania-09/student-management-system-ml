version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      # Map the database so it persists between container restarts
      - ./students.db:/app/students.db
    environment:
      - PYTHONUNBUFFERED=1
